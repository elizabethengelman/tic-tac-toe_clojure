; (ns tic-tac-toe_clojure.core-spec
;   (:require [speclj.core :refer :all]
;             [tic_tac_toe_clojure.core :refer :all]))

; (describe "tic-tac-toe_clojure.core"
;   (around [it]
;     (with-out-str (it)))
  
;   ; (it "gets a message from the user"
;   ;   (should= "first"
;   ;   (with-in-str "first"
;   ;   (get-input-from-user))))

;   ; (it "displays an empty board"
;   ;   (should= "__|__|__\n__|__|__\n  |  |  \n"
;   ;   (with-out-str
;   ;   (display-empty-board))))

;   ; (it "displays a numbered board"
;   ;   (should= " 1 | 2 | 3 \n-----------\n 4 | 5 | 6 \n-----------\n 7 | 8 | 9 \n"
;   ;   (with-out-str
;   ;   (display-numbered-board)))) 
; )

